extends: spectral:oas
rules:
  servers-url:
    description: "Checks if all URLs in 'servers' have the domain 'test.internal'."
    severity: error
    given: $.servers[*].url
    then:
      function: pattern
      functionOptions:
        match: https?://.*\.test\.internal(/.*)*
